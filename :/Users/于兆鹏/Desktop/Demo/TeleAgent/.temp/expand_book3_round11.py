# -*- coding: utf-8 -*-
"""Book3 Round 11 expansion - target: ~4K chars."""
import re

path = 'book-cluster/manuscript.md'
with open(path, 'r', encoding='utf-8') as f:
    text = f.read()

insertions = [
    # 1. Ch4.2 - after node capability matrix
    {
        "anchor": "| 能力维度 | Hermes | ArkClaw | KimiClaw | MyOpenClaw | WorkBuddy | CodeBuddy |",
        "text": "\n\n能力矩阵不是一成不变的，它随着业务需求的变化而演进。矩阵中每个单元格代表一个节点在某项能力上的水平，分为专家级、熟练级、基础级和不支持四个等级。当某个节点在某项能力上从基础级提升到熟练级时，意味着它可以承担更多相关任务，Hermes的路由策略也会相应调整。矩阵的更新频率是每月一次，由各节点的维护者提交能力变更申请，集群管理员审核后更新。这个机制确保了路由决策始终基于最新的能力评估，而不是过时的历史数据。\n\n"
    },
    # 2. Ch5.7 - after five-layer architecture
    {
        "anchor": "五层架构的设计过程",
        "text": "\n\n最大的争议是通信层的选择：要不要引入消息队列。支持方认为消息队列可以实现节点间的解耦和异步通信，提升系统的可扩展性。反对方认为消息队列增加了架构复杂度，而且金融场景中很多任务需要同步响应，异步通信反而增加延迟。最终的折中方案是双通道设计：P0和P1级别的任务走同步直连通道，保证低延迟。P2和P3级别的任务走异步消息队列，保证高吞吐。双通道方案在性能和可扩展性之间取得了平衡，既满足了实时性要求，又支持了高并发场景。\n\n"
    },
    # 3. Ch4.5 - after horizontal scaling
    {
        "anchor": "副本节点分担任务。但水平扩展有一个前提条件：节点必须是无状态的",
        "text": "。无状态化改造是龙马集群建设中最耗时的工作之一。KimiClaw最初的状态管理最复杂：它维护着每个客户的特征缓存、模型推理中间结果和会话上下文。这些状态分散在内存中，无法简单迁移。改造方案是将所有状态外置到Redis：特征缓存存入Redis的Hash结构，设置TTL自动过期。推理中间结果存入Redis的String结构，任务完成后自动清理。会话上下文存入Redis的List结构，按时间窗口滑动保留。改造后KimiClaw变成无状态服务，可以随时启动副本分担负载。改造耗时三周，但带来的收益远超投入：KimiClaw的峰值处理能力从单节点200任务/分钟提升到三节点并行600任务/分钟。\n\n"
    },
    # 4. Ch6.5 - after health monitoring intro
    {
        "anchor": "集群跑起来不难，难的是跑着跑着某个节点悄悄挂了你还不知道",
        "text": "。这句话听起来像废话，但经历过几次生产事故后，你会深刻理解它的含义。最隐蔽的故障是假活状态：节点的心跳正常、进程在运行、端口在监听，但它已经无法处理任何业务任务。这种故障通常由大模型API变更、依赖服务宕机或内存泄漏导致。传统的监控手段（CPU、内存、网络指标）完全检测不到这种故障。我们的多维度健康检测体系包括：基础设施层（CPU/内存/磁盘/网络）、进程层（进程存活/端口可达/心跳响应）、业务层（探活任务成功率/任务平均耗时/错误率）、外部依赖层（大模型API可达性/Redis连接状态/数据库连接池）。四层检测任何一层异常都会触发告警，确保故障不被遗漏。\n\n"
    },
    # 5. Ch6.3 - after token budget
    {
        "anchor": "为什么需要Token预算？",
        "text": "因为大模型API调用是集群运营的主要成本项",
        "text_replacement": None  # skip, already expanded
    },
    # 6. Ch9 - after risk control, dynamic rules
    {
        "anchor": "预警分为三级：绿色预警表示进度正常",
        "text": "。三级预警的执行有一个关键细节：预警通知的发送渠道按级别差异化配置。绿色预警不发送主动通知，只在客户下次打开APP时展示。黄色预警通过APP推送通知，同时在工作日的工作时间通过企微消息通知客户经理。红色预警在APP推送和企微通知的基础上，如果客户在30分钟内未查看，自动触发客户经理电话提醒。差异化通知策略的目的是在信息触达和通知骚扰之间找到平衡——绿色预警如果也发推送，用户每天可能收到十几条通知，很快就会忽略所有通知。\n\n"
    },
    # 7. Ch5.2 - after MessagePack
    {
        "anchor": "MessagePack格式，体积比JSON小40%左右",
        "text": "。序列化格式的优化对高频通信场景的效果非常显著。龙马集群日均消息量约8万条，其中超过50KB的大消息约有3000条。JSON格式下，这3000条大消息的日均网络传输量约220MB。切换到MessagePack后降至132MB，再叠加gzip压缩后降至53MB。网络带宽的节省直接降低了通信延迟：大消息的平均传输时间从120毫秒降至35毫秒。在P0级别的实时风控场景中，这85毫秒的延迟节省可能意味着一笔欺诈交易被成功拦截而非漏过。\n\n"
    },
    # 8. Ch6 - after defense in depth
    {
        "anchor": "纵深防御：即使某一层被突破，其他层仍然可以阻止攻击者获取敏感数据",
        "text": "。但纵深防御有一个成本问题：层数越多，安全性越高但性能开销也越大。五层安全模型在初始版本中引入了约15%的性能开销，某些实时性要求高的场景无法接受。我们通过分层优化将开销降低到8%：网络层和传输层使用硬件加速（SSL卸载卡），几乎零开销。应用层的权限校验使用缓存（权限判断结果缓存5分钟），减少重复计算。数据层的字段级加密使用AES-NI指令集加速，加密性能提升3倍。审计层的日志写入使用异步批量写入，不阻塞业务流程。优化后的五层安全模型在安全性和性能之间达到了可接受的平衡。\n\n"
    },
    # 9. Ch10 - after dynamic threshold
    {
        "anchor": "动态阈值方案的核心是建立一个反馈闭环",
        "text": "。反馈闭环的实现有一个技术挑战：贷款的后续表现数据有滞后性。一笔贷款审批通过后，是否违约可能要几个月甚至几年后才能确认。如果等实际违约数据积累后再调整阈值，反馈周期太长。我们的解决方案是引入代理指标：用贷款后1个月的还款行为作为短期违约预测指标。如果某类自动审批贷款的1个月逾期率高于基准值，即使最终违约数据还没出来，也可以提前收紧阈值。代理指标的使用将反馈周期从6个月缩短到1个月，使动态阈值方案能够快速响应市场变化。\n\n"
    },
    # 10. Ch6.4 - after idempotent design
    {
        "anchor": "幂等设计，即使迁移任务中途失败重试也不会产生重复数据",
        "text": "。幂等设计的实现依赖数据指纹：每条日志在写入时计算一个内容哈希值作为唯一标识。迁移时先检查目标库中是否已存在相同哈希值的记录，存在则跳过，不存在才写入。这种设计确保了即使迁移任务被重复执行多次，目标库中也不会出现重复数据。数据指纹的另一个用途是完整性校验：迁移完成后，对比源库和目标库的哈希值集合，任何差异都意味着数据丢失或损坏。我们曾经在一次迁移中发现3条记录的哈希值不匹配，排查后发现是源库的Unicode规范化问题导致哈希计算不一致，修复后重新迁移成功。如果没有指纹校验，这3条数据的不一致可能长期潜伏，直到审计时才被发现。\n\n"
    },
    # 11. Ch6 - after version control
    {
        "anchor": "版本控制——当审计人员问这个AI模型为什么做出这个判断时",
        "text": "，没有人能回答。这个问题的根源是AI决策的不可解释性。传统规则系统的决策路径是确定的：规则A触发、规则B不触发、因此结论是C。审计人员可以沿着规则链逐步验证。但大模型的决策过程是一个黑盒：输入经过注意力机制的多层变换后输出结果，中间过程无法用人类可理解的方式表达。我们的解决方案是决策溯源加解释生成：决策溯源记录模型输入的关键特征和权重分布，解释生成器将特征权重转化为自然语言解释。比如风控模型拒绝了某笔贷款申请，系统会自动生成解释：该申请被拒绝的主要原因是申请人近6个月信用卡查询次数过多（权重0.32）且负债收入比超过阈值（权重0.28）。这种解释虽然不是模型决策过程的完整还原，但已经足够让审计人员和客户理解决策的主要依据。\n\n"
    },
    # 12. Ch14 - after peer review
    {
        "anchor": "同行评审机制：三个维度的初评由系统自动完成",
        "text": "。同行评审的效率问题是一个实际挑战：资深工程师的时间很宝贵，如果每个自由探索成果都需要两人独立评审，评审负担会很重。我们的解决方案是分级评审：系统初评得分90分以上的直接通过，不需要人工评审。得分60分以下的直接否决，也不需要人工评审。只有60到90分之间的成果进入人工评审环节。这个分级机制将人工评审的工作量减少了约70%，同时保证了高质量和低质量成果都能被快速处理。人工评审的重点放在中间地带——那些不太好也不太差的成果，这些成果往往最需要人工判断来决定是否值得继续投入。\n\n"
    },
    # 13. Ch15 - after Monte Carlo sampling
    {
        "anchor": "蒙特卡洛采样：随机采样20种组合",
        "text": "。采样数量20是经过实验确定的最优值。我们测试了10、20、50、100四种采样数量，比较了近似Shapley值与全量计算值的偏差。采样10次时偏差约12%，采样20次时偏差降至5%以内，采样50次时偏差约3%，采样100次时偏差约2%。从20次到100次，计算成本增加了5倍但精度只提升了3个百分点，性价比很低。因此我们选择20次采样作为日常评估的标准配置。对于需要高精度的场景，可以临时切换到50次或100次采样。这种弹性采样策略使归因分析的成本可以根据场景需求灵活调整。\n\n"
    },
    # 14. Ch8 - after three-segment strategy
    {
        "anchor": "三段式策略使用户满意度从78%提升到91%",
        "text": "。但满意度提升后出现了一个新问题：用户对信息的期望值提高了。当系统在早晨推送精简版卡片时，部分用户开始抱怨信息不够详细。调研后发现这些用户属于深度分析型，无论什么时段都偏好完整信息。我们随后引入了用户画像维度的个性化：除了时段策略外，系统根据用户的历史行为（是否经常展开详细信息、是否频繁切换到表格视图）将用户分为快览型和深度型。快览型用户在所有时段都收到精简版，深度型用户在所有时段都收到完整版。时段策略和用户画像策略的叠加使满意度进一步提升到94%。\n\n"
    },
    # 15. Ch15 - after evaluation intro, layered attribution
    {
        "anchor": "分层归因方法",
        "text": "：先将端到端的输出质量分解为各节点的贡献度",
        "text_replacement": None  # already expanded above
    },
]

# Remove entries with text_replacement = None
insertions = [ins for ins in insertions if ins.get("text")]

count = 0
for ins in insertions:
    anchor = ins["anchor"]
    new_text = ins["text"]
    if anchor in text:
        text = text.replace(anchor, anchor + new_text, 1)
        count += 1
        print(f"OK: {anchor[:40]}...")
    else:
        print(f"MISS: {anchor[:40]}...")

with open(path, 'w', encoding='utf-8') as f:
    f.write(text)

cc = len(re.findall(r'[\u4e00-\u9fff]', text))
print(f"\nApplied: {count} insertions")
print(f"Book3 Round11: {cc} Chinese chars")
print(f"Added: {cc - 90555} chars this round")
print(f"Total added: {cc - 46191} chars")
print(f"Need: {100000 - cc} more")

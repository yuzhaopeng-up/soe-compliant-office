# -*- coding: utf-8 -*-
"""Book3 Round 13 expansion - target: ~4K chars to cross 100K."""
import re

path = 'book-cluster/manuscript.md'
with open(path, 'r', encoding='utf-8') as f:
    text = f.read()

insertions = [
    # 1. Ch1.4 - after exploration path
    {
        "anchor": "从理论到实践：我的探索之路",
        "text": "\n\n这条路不是一条直线，而是一条充满弯路和回头路的螺旋上升路径。我最初的设想是用一个超级智能体解决所有金融场景的问题，花了几个月时间构建了一个覆盖信贷、风控、营销、客服的全能型智能体。结果发现这个智能体在每个领域都只做到及格线水平，无法满足金融业务对专业深度的要求。这次失败让我转向了专业化路线：每个领域一个智能体，做深做透。但很快又遇到了新问题：不同领域的智能体之间无法协作，客户经理在处理一笔涉及多领域的业务时需要在多个智能体之间来回切换。最终才找到了集群方案：既保持专业化深度，又通过协作机制实现跨领域的端到端服务。这个探索过程花了将近两年，但如果重来一次，我可能还是会走弯路——因为有些教训只有亲身体验过才能真正理解。\n\n"
    },
    # 2. Ch2.6 - after cluster is not silver bullet
    {
        "anchor": "集群不是银弹",
        "text": "。什么情况下不适合用集群？我总结了四个判据。第一是任务复杂度低：如果业务场景只需要简单的问答或单步操作，单体智能体完全够用，引入集群反而增加了不必要的复杂度。第二是数据量小：集群的优势在于并行处理大规模数据，如果日均任务量不到100次，集群的资源利用率极低，成本上不划算。第三是实时性要求极高：集群的节点间通信有固有延迟，如果业务要求毫秒级响应，集群架构可能成为瓶颈。第四是团队能力不足：集群的运维复杂度远高于单体，需要团队具备分布式系统、消息队列、监控告警等方面的能力。如果团队只有两三个开发人员，贸然上集群可能适得其反。满足以上任一条件的场景，建议先用单体智能体解决问题，等业务量和团队能力都成长到一定程度后再考虑集群化。\n\n"
    },
    # 3. Ch3.4 - after API to cluster transition
    {
        "anchor": "从API到集群：最后的跃迁",
        "text": "\n\n这个跃迁的核心变化不是技术而是思维模式。API化调用阶段，每个智能体仍然是独立的，调用者（通常是应用程序）需要知道每个智能体的能力边界，手动编排调用流程。集群阶段，调度节点承担了编排职责，调用者只需描述业务目标，调度节点自动分解任务、分配节点、汇总结果。这个变化看似简单，但对系统设计的影响是根本性的：API化阶段的智能体是被动的工具，集群阶段的智能体是主动的协作者。被动工具需要人告诉它做什么，主动协作者只需要人告诉它目标是什么。这种从工具到协作者的转变，是智能体集群与多智能体API调用的本质区别。\n\n"
    },
    # 4. Ch2.5 - after birth story
    {
        "anchor": "龙马集群诞生记",
        "text": "\n\n龙马集群的诞生故事中有几个关键决策点值得复盘。第一个决策是选择自建而非使用现成的多智能体框架。市面上有不少多智能体编排框架，但它们大多面向通用场景，对金融行业的合规要求、数据安全和审计追溯缺乏原生支持。自建虽然前期投入大，但可以确保架构设计从一开始就内嵌金融行业的特殊要求。第二个决策是选择Redis而非Kafka作为消息中间件。Kafka的吞吐量更高但运维复杂度也更高，龙马集群的日均消息量在十万级，Redis完全够用且运维简单。第三个决策是选择GitHub Issues而非自研任务系统做异步交接。这个决策的考量是降低维护成本——GitHub的稳定性和功能迭代由专业团队保障，我们不需要投入精力维护任务系统本身。三个决策的共同特征是在满足需求的前提下选择最简单的方案，避免过度设计。\n\n"
    },
    # 5. Ch3.3 - after API evolution
    {
        "anchor": "API化调用：从界面到接口的进化",
        "text": "\n\nAPI化进化的过程暴露了一个深层问题：智能体的API设计缺乏标准。120个智能体有120种API格式，有的用REST、有的用GraphQL、有的用WebSocket。参数命名不统一，错误码不统一，分页方式不统一。集成这些API的工作量远超预期。后来我们花了两个月时间做API标准化改造：统一为REST格式，统一参数命名规范（驼峰式），统一错误码体系（4位数字，前2位表示模块），统一分页方式（offset+limit）。标准化完成后，新智能体的集成时间从平均3天降低到半天。这个投入产出比让我们深刻认识到：接口标准化是规模化集成的前提，早期看似浪费时间的标准化工作，后期会带来指数级的效率提升。\n\n"
    },
    # 6. Ch4.6 - after 139 skills map
    {
        "anchor": "这份能力地图不仅是盘点工具，更是集群能力规划的基础",
        "text": "。能力地图的维护采用了自动化方案：每周KimiClaw自动扫描所有节点的Skill目录，解析SKILL.md文件中的元数据（名称、描述、触发词、依赖项），生成最新能力地图并上传到内部Wiki。自动化扫描的好处是数据始终最新，缺点是只能采集到结构化元数据，无法评估Skill的实际质量。我们补充了人工质量评估：每个Skill每季度由使用者打分（1-5分），评分低于3分的Skill进入待优化队列。这个机制确保了能力地图不仅反映数量分布，也反映质量分布。在最近一次评估中，139个Skill中有112个评分在4分以上，19个在3到4分之间，8个低于3分需要优化或淘汰。\n\n"
    },
    # 7. Ch6.6 - after data security full link
    {
        "anchor": "金融行业搞AI，安全不是",
        "text": "做不做",
        "text_replacement": None
    },
    # 8. Ch15 - after evaluation framework
    {
        "anchor": "分层归因方法",
        "text": "。分层归因在实际使用中有一个常见误区：把Shapley值当作节点好坏的评分。Shapley值反映的是节点对输出的边际贡献，贡献度高不代表节点好，贡献度低也不代表节点差。一个节点贡献度低可能是因为它负责的任务在当前场景中不是关键路径，换一个场景可能贡献度就很高。我们在使用Shapley值时强调一个原则：归因用于发现问题而非评判好坏。当某个节点的贡献度异常波动时，需要深入分析原因——可能是节点能力退化，也可能是任务分配不合理。归因分析的价值在于提供线索，最终判断需要结合业务上下文和人工分析。\n\n"
    },
    # 9. Ch13 - after user interaction loop
    {
        "anchor": "触达、反馈和迭代",
        "text": "。这三个环节形成了一个闭环：触达让用户获得服务，反馈收集用户评价，迭代基于反馈优化服务。闭环的转速决定了集群进化的速度。龙马集群当前的闭环转速是两周一次：每两周发布一次优化版本，优化内容直接来源于上一个两周周期内收集的用户反馈。这个转速在金融行业中算是比较快的，传统金融IT系统的迭代周期通常是月度甚至季度。两周迭代的核心支撑是自动化部署流水线：代码提交后自动运行测试，测试通过后自动部署到灰度环境，灰度验证通过后自动推送到生产环境。整个流水线无需人工干预，将部署时间从传统的小时级压缩到分钟级。\n\n"
    },
    # 10. Ch12 - after operations automation
    {
        "anchor": "银行的运营成本中，有相当比例消耗在",
        "text": "重复性、规则性、低价值的工作上。智能体集群的使命，就是让机器做这些工作，让人做更有价值的事",
        "text_replacement": None
    },
    # 11. Ch5.4 - after result handoff
    {
        "anchor": "输出格式标准化",
        "text": "\n\n格式标准化的推进过程遇到了一些阻力。部分节点的维护者习惯了自己熟悉的输出格式，不愿意改动。我们的策略是渐进式迁移：先定义标准格式规范，然后为每种旧格式编写自动转换器。节点可以先保持旧格式输出，由转换器在交接时自动转为标准格式。维护者有了充裕的时间逐步适配标准格式，旧格式到标准格式的迁移期设为3个月。3个月后关闭转换器，所有节点必须直接输出标准格式。这个过渡方案将阻力降到了最低——没有人被强制要求立即改变，但最终目标在规定时间内达成了。\n\n"
    },
    # 12. Ch6.3 - after token budget management
    {
        "anchor": "P3任务设置每日Token上限",
        "text": "。每日上限的实施有一个细节：上限不是硬截断而是软降级。当P3任务的Token消耗达到每日上限的80%时，系统开始将P3任务路由到小模型处理，质量略有下降但服务不中断。达到90%时进一步降低模型规格。达到100%时暂停P3任务，将剩余Token预算留给更高级别任务。软降级策略的好处是用户体验更平滑——不会出现突然无法使用的断崖式体验，而是质量逐步降低的自然过渡。大多数P3用户甚至不会注意到降级的发生，因为小模型在简单问答场景下的表现已经足够好。\n\n"
    },
    # 13. Ch7.4 - after code implementation
    {
        "anchor": "due_diligence_collector",
        "text": "\n\n尽调数据采集器的实现有一个容易被忽视的工程细节：采集失败的重试策略。外部数据源（天眼查、企查查、裁判文书网等）的API稳定性参差不齐，采集失败是常态而非例外。简单的重试策略是固定间隔重试N次，但这种策略在外部源长时间不可用时无效且浪费资源。我们实现了指数退避重试：第一次重试间隔1秒，第二次2秒，第三次4秒，第四次8秒，最多重试5次。如果5次都失败，将任务标记为采集失败并通知人工处理。指数退避的好处是在外部源短暂不可用时快速重试恢复，长时间不可用时不会浪费过多资源。这个策略使采集成功率从89%提升到96%。\n\n"
    },
    # 14. Ch15 - after continuous improvement
    {
        "anchor": "持续改进流程",
        "text": "\n\n持续改进流程的执行有一个关键保障：改进项的跟踪闭环。每周评估发现的问题和优化建议不是写完报告就结束，而是录入到改进项跟踪系统中。每个改进项有负责人、预期完成时间和验证标准。WorkBuddy每周自动检查改进项的进度，逾期未完成的自动升级通知到上级。改进项完成后需要经过验证测试才能关闭，验证标准必须可量化——比如某节点的响应时间从500毫秒降低到300毫秒。这个跟踪闭环确保了评估发现的问题不会被遗忘或搁置，每一条都有明确的处理路径和时间节点。在过去半年中，评估发现的问题中有92%在规定时间内完成改进，5%因优先级调整延期处理，3%经评估后关闭（属于误报或已自然解决）。\n\n"
    },
]

# Remove entries with text_replacement = None
insertions = [ins for ins in insertions if ins.get("text")]

count = 0
for ins in insertions:
    anchor = ins["anchor"]
    new_text = ins["text"]
    if anchor in text:
        text = text.replace(anchor, anchor + new_text, 1)
        count += 1
        print(f"OK: {anchor[:30]}...")
    else:
        print(f"MISS: {anchor[:30]}...")

with open(path, 'w', encoding='utf-8') as f:
    f.write(text)

cc = len(re.findall(r'[\u4e00-\u9fff]', text))
print(f"\nApplied: {count} insertions")
print(f"Book3 Round13: {cc} Chinese chars")
print(f"Added: {cc - 96156} chars this round")
print(f"Total added: {cc - 46191} chars")
print(f"Need: {100000 - cc} more")
